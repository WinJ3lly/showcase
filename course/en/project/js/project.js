define([
	"core/js/adapt",
	"./helpers"
  ], function(Adapt) {
	
	/*class Project extends Backbone.Model {

		initialize() {
			Adapt.on({
				'app:dataReady': this.onDataReady.bind(this)
			});
		}

		onDataReady() {

			Adapt.on("componentView:postRender", (componentView) => {

				const id = componentView.model.get("_id");

				switch (id) {
					case "c-X-0":
						// Delay necessary due to Handlebars helperEl 1 frame render delay.
						_.delay(() => {
							// Insert new functions here
						}, 100);
						break;
				}
			});
		}

		// exampleFunction(componentView) {
		// 	componentView.listenTo(componentView.model.getChildren(), {
		// 		"change:_isVisited": componentView.setCompletionStatus
		// 	})
		// }

	}

	new Project(); */

});

